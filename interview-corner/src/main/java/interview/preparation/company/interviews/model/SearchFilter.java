package interview.preparation.company.interviews.model;

public enum SearchFilter {
    DATE_RANGE(0),
    AMOUNT(1),
    VENDOR(2),
    CATEGORY(3),
    VENDORE_CATEGORY(23);

    SearchFilter(int i) {
    }
}